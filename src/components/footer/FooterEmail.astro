---
import { contactData } from "@config/contact";
---

<p class="py-2 text-primaryMuted">
    <a
        class="text-foreground hover:text-foreground/80"
        href={`mailto:${contactData.email}`}
    >
        {contactData.email}
    </a>
</p>
